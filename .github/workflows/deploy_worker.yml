on:
  workflow_dispatch:
    inputs:
      tags:
        description: 'trigger workflow'
  push:
    branches:
      - main
      - release/*

jobs:
  deploy:
    runs-on: ubuntu-latest
    name: Deploy
    steps:
      - uses: actions/checkout@v2
      - name: yarn install
        uses: Jaid/action-npm-install@1.2.1
        with:
          packageManager: "yarn"
      - name: Deploy API
        uses: cloudflare/wrangler-action@2.0.0
        with:
          apiToken: ${{ secrets.CF_API_TOKEN }}
          workingDirectory: './packages/api'
