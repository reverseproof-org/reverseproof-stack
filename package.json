{
  "name": "reverseproof-stack",
  "version": "0.0.0",
  "scripts": {
    "dev": "nx run-many --target=dev",
    "lint": "nx run-many --target=lint --verbose",
    "test": "nx run-many --target=test --verbose"
  },
  "private": true,
  "dependencies": {},
  "devDependencies": {
    "@nrwl/nx-cloud": "latest",
    "nx": "15.2.1",
    "prettier": "^2.6.2"
  },
  "workspaces": [
    "packages/*"
  ]
}
